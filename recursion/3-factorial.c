#include "main.h"
/**
 * factorial - factorial to print using recursion
 * @n: integer to use
 * Return: factorial
 */
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	if (n == 0)
	{
		return (1);
	}
	return (n * factorial(n - 1));
}
