#include <stdio.h>
/**
 * main - entry point
 *
 * specifies return point
 * Return: 0
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	return (0);
	putchar('\n');
}
