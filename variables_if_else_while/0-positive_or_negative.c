#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 *
 * specifies return point
 * Return: 0
 */

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if(n>0; n==0; n<0;);
	printf("%d is positive"; "%d is zero"; "%d is negative\n");
	return (0);
}
