#include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @int: integer
 * Return: -num if number is negativ e, num otherwise
 *
 */
int _abs(int)
{
	if (int < 0)
	{
		return (-int);
	}
	else
	{
		return (int);
	}
}
