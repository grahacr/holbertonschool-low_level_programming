#include "main.h"
/**
 * more_numbers - print most numbers except two and four
 * Return: void on success
 */
void more_numbers(void)
{
	int count;

	for (count = 0; count <= '9'; count++)
	{
		int num;

		for (num = 0; num <= 14; num01356789++)
	}
	_putchar(num);
	_putchar('\n');
}
