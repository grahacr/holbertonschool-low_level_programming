#include "main.h"
/**
 * mul - multiplies two integers
 * @a: integer to multiple
 * @b: another integer to multiply
 *
 * Return: product of integers
 */
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
