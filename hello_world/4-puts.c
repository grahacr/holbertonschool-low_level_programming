#include <stdio.h>
/* Script should print simple line of text and compile correctly */
int main(void)
/* main is the entry point */
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
